name: Gazebo Go project
on:
  push:
    branches: [ $default-branch ]
  pull_request:

jobs:
  go:
    uses: gazebo-web/actions/.github/workflows/go.yaml@a9e2269939bc98700bc7a31c7d83e4cc050a18dc
    with:
      go-version: '1.19'
      database: 'mysql'